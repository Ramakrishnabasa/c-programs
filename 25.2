#include <stdio.h>
void main()
{
    char str[100];
    int i;
    printf("string:");
    gets(str);
    for(i=0; str[i]!='\0'; i++)
    {
        if(i%2==1)
        {
            str[i]=str[i]-32;
        }
    }
    puts(str);
}
Out put:
string:coding
cOdInG
